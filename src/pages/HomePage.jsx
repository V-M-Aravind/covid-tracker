import HomePageComponent from '../components/HomePage';
import Layout from '../layout';

const HomePage = () => {
  return (
    <Layout>
      <HomePageComponent />
    </Layout>
  );
};

export default HomePage;
